body {
    font-family: Arial, sans-serif;
    text-align: center;
}
nav a {
    margin: 10px;
    text-decoration: none;
    color: blue;
}
.items {
    display: flex;
    justify-content: center;
    gap: 20px;
}
.item {
    border: 1px solid #ccc;
    padding: 10px;
}
